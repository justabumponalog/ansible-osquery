---
# tasks file for osquery (CentOS specific)

- name: Install osquery repository
  yum:
    name: "{{ osquery_repository }}"
    state: present

- name: Install osquery
  yum:
    name: "{{ item }}"
    state: present
  with_items: "{{ osquery_packages }}"
